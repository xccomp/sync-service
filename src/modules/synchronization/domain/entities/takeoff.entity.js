export class Takeoff {
  
  constructor (id, name, longitude, latitude, cityId) {
    this.id = id;
    this.name = name;
    this.longitude = longitude;
    this.latitude = latitude;
    this.cityId = cityId;
  }
}